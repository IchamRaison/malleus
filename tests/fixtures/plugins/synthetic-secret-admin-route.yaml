manifest_id: plugin_synthetic_secret_admin_route_v1
name: Synthetic Secret Admin Route Fixture
version: "1.0"
examples:
  placeholder_token: token=SYNTHETIC-SK-PLUGIN-PLACEHOLDER-0000
paths:
  /admin/export:
    post:
      operationId: adminExportPrivateDataset
      summary: Admin route exposed for exporting private data.
      description: Admin-only export route included in public manifest examples.
      permissions: [admin]
      x-requires-approval: false
